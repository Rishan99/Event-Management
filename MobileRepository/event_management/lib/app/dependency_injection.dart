import 'package:get_it/get_it.dart';

import '../dependency_injection.dart';

setupViewModel() {
  ///[View Model]
  // locator.registerFactory<DashboardController>(() => DashboardController());
  // locator.registerFactory<HomeTabCtrl>(() => HomeTabCtrl(preferenceService));
  // locator.registerFactory<NoteController>(() => NoteController(locator<NoteService>()));
  // locator.registerFactory<ForgotPasswordController>(() => ForgotPasswordController());
  // locator.registerFactory<LoginController>(() => LoginController());
  // locator.registerFactory<NotificationController>(() => NotificationController());
  // locator.registerFactory<ProfilePageController>(() => ProfilePageController(preferenceService));
  // locator.registerFactory<NewStaffPageController>(() => NewStaffPageController());
  // locator.registerFactory<StaffDetailPageController>(() => StaffDetailPageController());
}
